let circle = {};
console.log(circle);

/**
 * JavaScript engine has garbage collection
 *
 * Memory allocation is automatic, under the hood.
 * Uses complex algorithms to determine what is no
 * longer used and deallocates it from memory.
 */
