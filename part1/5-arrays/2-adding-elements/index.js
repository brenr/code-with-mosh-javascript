const numbers = [3, 4];

/**
 * Add elements to the array at the...
 */

// End
numbers.push(5, 6);

// Beginning
numbers.unshift(1, 2);

// Middle (seldom used but can has its use cases)
numbers.splice(2, 0, 'a', 'b');
