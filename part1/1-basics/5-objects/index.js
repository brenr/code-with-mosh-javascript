let person = {
  name: 'Brendan',
  age: 24
};

console.log(person);

/**
 * Notes
 *
 * object data may be accessed via....
 *
 * Dot notation:
 * person.name = 'Joe';
 *
 * Bracket notation:
 * let selection = 'name';
 * person[selection] = 'Joe';
 *
 * Bracket notation is used if you're trying to get a property of an
 * object but you don't know what it is at runtime (like in iterators)
 */
