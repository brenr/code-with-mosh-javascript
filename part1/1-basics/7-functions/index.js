function greet(name, lastName) {
  console.log('Hello, ' + name + ' ' + lastName);
}

greet('Brendan', 'Rosa');

/**
 * Notes
 *
 * Parameters are the values used within the function
 * Arguments are the actual implementation when the function is invoked
 */
