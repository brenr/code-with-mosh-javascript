const interestRate = 0.5;
interestRate = 1;
console.log(interestRate);

/**
 * Notes
 *
 * Like variables, constants are final and are immutable.
 * Re-assigning a value to a primitive constant throws an error on line 3.
 */
